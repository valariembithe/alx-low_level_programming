#include "main.h"

/**
 * _abs - Computes absolute integer
 * @n: The integer to be checked
 *
 * Return: absolute value is integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
